# config.yml
backend:
  name: git-gateway
  branch: master # Branch to update (optional; defaults to master)
media_folder: 'assets/uploads'
collections:
  - name: 'blog'
    label: '文章'
    folder: '_posts/'
    create: true
    slug: '{{year}}-{{month}}-{{day}}-{{slug}}'
    editor:
      preview: true
    fields:
      - { label: '布局', name: 'layout', widget: 'hidden', default: 'post' }
      - { label: '标题', name: 'title', widget: 'string' }
      - { label: '日期', name: 'date', widget: 'datetime' }
      - { label: '摘要', name: 'abstract', widget: 'string', required: false }
      - { label: '封面', name: 'cover', widget: 'image' }
      - { label: '分类', name: 'category', widget: 'select', multiple: true, options: ["最新动态", "公益故事", "视频中心", "媒体报道"]  }
      - { label: '标签', name: 'tags', widget: 'list', required: false }
      - { label: '正文', name: 'body', widget: 'markdown' }